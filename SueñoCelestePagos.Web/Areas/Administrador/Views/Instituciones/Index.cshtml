@model IEnumerable<SueñoCelestePagos.Entities.Institucion>

@{
    ViewBag.Title = "Index";
}

<!--BEGIN TITLE & BREADCRUMB PAGE--><!--BEGIN TITLE & BREADCRUMB PAGE-->
<h1 class="page-header">Instituciones</h1>
<ul class="breadcrumb">
    <li><span class="glyphicon glyphicon-home">&nbsp;</span>Home</li>
    <li><a href="#">Instituciones</a></li>
</ul>
<!--END TITLE & BREADCRUMB PAGE-->

<hr />

<div>
    <div class="col-md-8">
        @*@using (Html.BeginForm())
        {
            <div class="col-md-6">
                <input type="text" name="searchString" id="searchString" class="form-control" value="@ViewBag.CurrentFilter " />
            </div>
            <div class="col-md-2">
                <input type="submit" value="Buscar" class="btn btn-primary" />
            </div>
        }*@
    </div>
    <div class="col-md-4">
        <a href="@Url.Action("Create", "Instituciones")" class="btn btn-primary">Agregar Nueva Institucion</a>
    </div>
    <div class="clearfix">
    </div>
</div>

<hr />

<!--BEGIN CONTENT-->
<div class="page-content">
    <div id="tab-general">
        @if (Model.Count() > 0)
        {
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Nombre)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Cuit)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Domicilio)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Telefono)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nombre)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Cuit)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Domicilio)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Telefono)
                        </td>
                        <td>
                            @Html.ActionLink("Editar", "Edit", new { id = item.ID }) |
                            @Html.ActionLink("Detalles", "Details", new { id = item.ID }) |
                            @Html.ActionLink("Porcentaje de Aporte", "Aporte", new { id = item.ID })|
                            @Html.ActionLink("Eliminar", "Delete", new { id = item.ID })
                        </td>
                    </tr>
                }

            </table>


        }
        else
        {
            <div class="col-md-12">
                <div class="panel">
                    <div class="panel-heading">
                        <h3 class="text-center">Su Busqueda no dio Resultados</h3>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
<!--END CONTENT-->
